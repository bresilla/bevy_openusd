#usda 1.0
(
    defaultPrim = "World"
    upAxis = "Y"
    metersPerUnit = 1.0
    doc = "Smallest viable USD stage — one empty Xform under the default prim. Used by M0 integration tests."
)

def Xform "World"
{
}
